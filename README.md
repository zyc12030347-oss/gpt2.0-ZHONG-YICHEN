# GPT 2.0 模型實作 (gpt2.0-ZHONG-YICHEN)

這是一個基於 PyTorch 實作的自定義 GPT (Generative Pre-trained Transformer) 語言模型專案。本專案包含了一個簡易的 Tokenizer、模型結構定義，以及文本生成的推論程式碼。

---

## 🛠️ 環境需求與安裝

在執行本專案之前，請確保你的電腦已安裝 Python 3.12+ 以及相關的深度學習套件：

```bash
pip install torch

